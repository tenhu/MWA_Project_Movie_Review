import { JwtDirective } from './jwt.directive';

describe('JwtDirective', () => {
  it('should create an instance', () => {
    const directive = new JwtDirective();
    expect(directive).toBeTruthy();
  });
});
